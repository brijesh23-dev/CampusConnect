# Production backend URL — deployed on Render
# This is the default. Locally, .env.local overrides this automatically (Vite loads .env.local with higher priority).
VITE_API_URL=https://campusconnect-dhyh.onrender.com